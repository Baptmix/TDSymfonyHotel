<div class="card border-primary mb-3" style="margin-top: 30px">
    <div class="card-header">Gestion Chambre</div>
    <div class="card-header">
{{ form_start(formChambre) }}
    <fieldset>
        <div class="form-group row">
            <label for="numero" class="col-sm-2 col-form-label">Numéro :</label>
            {{ form_widget(formChambre.numero, {'attr': {'placeholder': 'Numéro'}}) }}
    </div>
        <div class="form-group row">
            <label for="etage" class="col-sm-2 col-form-label">Étage :</label>
            {{ form_widget(formChambre.etage, {'attr': {'placeholder': 'Étage'}}) }}
        </div>
        <div class="form-group row">
            <label for="statut" class="col-sm-2 col-form-label">Statut :</label>
            {{ form_widget(formChambre.statut, {'attr': {'placeholder': 'Statut'}}) }}
        </div>
        <div class="form-group row">
            <label for="statut" class="col-sm-2 col-form-label">Assigné à :</label>
            {{ form_widget(formChambre.assignedTo, {'attr': {'placeholder': 'Assigné à'}}) }}
    </div>
    <div class="">
        {{ form_widget(formChambre.sauvegarder, {'attr': {'value': 'Enregistrer', 'class': 'btn btn-success'}}) }}
    </div>
    </fieldset>
{{ form_end(formChambre) }}
    </div>
</div>